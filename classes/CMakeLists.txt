set(SOURCES ${SOURCES} Debug.cxx)
set(SOURCES ${SOURCES} Element.cxx)
set(SOURCES ${SOURCES} Compound.cxx)
set(SOURCES ${SOURCES} Document.cxx)
set(SOURCES ${SOURCES} Page.cxx)
set(SOURCES ${SOURCES} Head.cxx)
set(SOURCES ${SOURCES} ParagraphText.cxx)
set(SOURCES ${SOURCES} AText.cxx)
set(SOURCES ${SOURCES} Image.cxx)
set(SOURCES ${SOURCES} CaptionedImage.cxx)
set(SOURCES ${SOURCES} DivColumn.cxx)
set(SOURCES ${SOURCES} Uri.cxx)
set(SOURCES ${SOURCES} Title.cxx)
set(SOURCES ${SOURCES} Div.cxx)
set(SOURCES ${SOURCES} Uri.cxx)
set(SOURCES ${SOURCES} CSSElement.cxx)
set(SOURCES ${SOURCES} CSSClass.cxx)
set(SOURCES ${SOURCES} Globals.cxx)
set(SOURCES ${SOURCES} MainMenu.cxx)
set(SOURCES ${SOURCES} Site.cxx)
set(SOURCES ${SOURCES} DocumentImpl.cxx)
set(SOURCES ${SOURCES} Home.cxx)
set(SOURCES ${SOURCES} Education.cxx)
set(SOURCES ${SOURCES} ProfExperience.cxx)
set(SOURCES ${SOURCES} Patents.cxx)

add_library(krap ${SOURCES})

target_include_directories(krap PRIVATE "${CgiCc_DIR}/include")
